name: lyric_search_module
description: A new Flutter package project.
version: 0.0.1
publish_to: "none"

environment:
  sdk: ">=2.7.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  # LOCAL PACKAGES
  shared:
    path: ../shared

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter: